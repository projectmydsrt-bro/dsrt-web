# Copy to .env and fill values. DO NOT commit .env to git.

# -----------------------
# Public (frontend-safe)
# -----------------------
# Public keys are safe to include in frontend builds
NEXT_PUBLIC_SUPABASE_URL=     # public: Supabase project URL (ex: https://xxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_ANON_KEY= # public: anon key for client usage
NEXT_PUBLIC_API_URL=          # optional: URL to your serverless API

# -----------------------
# Private (server-only)
# -----------------------
# These keys must never be exposed to the client or committed
SUPABASE_SERVICE_ROLE_KEY=    # server-only: service role key
REPLICATE_API_TOKEN=          # server-only: Replicate API token
VERCEL_TOKEN=                 # CI/deploy token (server-only)
CODECOV_TOKEN=                # optional: required for private repos to upload coverage
NPM_TOKEN=                    # optional: for publishing or semantic-release

# Notes:
# - Prefix client-safe variables with NEXT_PUBLIC_ (for Next.js) so they are intentionally exposed.
# - Remove values before committing; keep this file as example only.
